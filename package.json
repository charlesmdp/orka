{
  "name": "orka-landing",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:pages": "node scripts/build.mjs --pages",
    "check:supabase": "node --env-file-if-exists=.dev.vars scripts/check-supabase.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
