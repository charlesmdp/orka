import {installationGuides,exampleSnippet} from './installation-content.mjs';
import {bestChatGuides} from './best-chat-content.mjs';
import {comparisons} from './comparison-content.mjs';
import {research} from './comparison-research.mjs';
import {vendorMark} from './vendor-marks.mjs';
import {escapeHtml as e,signup,marineIcon,faqSection,faqSchema} from './site-chrome.mjs';
const guideDate='21 September 2026';
const link=(url,label)=>`<a href="${url}" target="_blank" rel="noopener">${e(label)} ↗</a>`;
const guideFooter=(sources)=>`<section class="ed-wrap guide-sources"><span class="ed-kicker">THE DOCUMENTATION BEHIND THE GUIDE</span><h2>Checked, with a way to check again.</h2><p>Valid as of September 2026. Official sources checked ${guideDate}. Platform menus, plans and prices can change. If we have something wrong, email <a href="mailto:hello@orka.chat">hello@orka.chat</a>.</p><ul>${[...new Map(sources.map(s=>[s[1],s])).values()].map(([name,url])=>`<li>${link(url,name)}</li>`).join('')}</ul></section>`;
const platformPill=g=>`<span class="platform-pill" style="--platform-color:${g.color}">${e(g.mark)}</span>`;
const installSlug=g=>'how-to-add-orka-to-'+g.id;
const bestSlug=g=>'best-live-chat-for-'+g.id;
function snippetPanel(){return `<div class="guide-snippet"><div><span>THE INSTALLATION SNIPPET</span><button type="button" data-copy-snippet="orka-example-code">Copy example</button></div><pre tabindex="0"><code id="orka-example-code" tabindex="-1">${e(exampleSnippet)}</code></pre><p><strong>YOUR_PROJECT_ID is a placeholder.</strong> Copy the complete snippet from your own project’s Installation screen. This example contains no customer’s project ID.</p></div>`;}
export function installationBody(g){
 const faqs=[
  [`Where do I find my Orka snippet?`,'Log in to the Orka dashboard, open Settings, select the project for this website and choose Installation. Click Copy Code in How to embed. Use that project’s full snippet, not the placeholder shown in this guide.'],
  [`What do I need on ${g.name}?`,g.requirement+' '+g.limit],
  ['Do I add it to every page?','Use the site-wide header or the shared layout so the script is included wherever you want chat. Add it once, not through both a plugin and a theme. Test the homepage and an inner page.'],
  ['Does every website need a different project ID?','Use a separate Orka project for each distinct product or store whose context you want to keep separate. Copy its own snippet. The conversations can still appear together in your shared inbox.'],
  ['Why is the widget missing?','Check the published site, confirm the project ID and complete script are present, and look for duplicate installations. Review your platform’s plan restrictions, cache, consent settings and browser extensions. If it still fails, send Orka the public page URL and the error you see.'],
  ['Does installing the snippet enable AI automatically?','Installing the widget is separate from choosing your knowledge and AI behavior. Configure the project, review its answers and decide whether you want human replies, AI drafts or automatic replies. AI usage is separate from the team plan.']
 ];
 return `<section class="guide-install-hero"><div class="ed-wrap"><a class="c-back" href="/installation">← All installation guides</a><div class="guide-install-grid"><div><span class="ed-kicker">${e(g.name.toUpperCase())} INSTALLATION · SEPTEMBER 2026</span><h1>How to add Orka<br>to <em>${e(g.name)}.</em></h1><p class="ed-lead">${e(g.headline)}</p><p>A small snippet in your site’s head.<br>A direct line to the humans behind your project.</p><a class="ed-button lime" href="#installation-steps">Follow the steps ↓</a></div><div class="guide-connect-art" aria-label="Illustration: connect your site to Orka"><div class="guide-platform">${platformPill(g)}<strong>${e(g.name)}</strong><small>Your website</small></div><span class="guide-connect-line" aria-hidden="true">→</span><div class="guide-platform"><img src="/assets/orka-logo.svg" width="56" height="52" alt="Orka"><strong>One inbox</strong><small>Every project</small></div><div class="guide-code-label"><code>&lt;head&gt; <b>your Orka snippet</b> &lt;/head&gt;</code></div><div class="guide-new-message"><span>●</span><div><small>YOUR ${e(g.name.toUpperCase())} SITE</small><strong>Hi! Can you help me get started?</strong></div></div></div></div></div></section><nav class="c-jump" aria-label="Installation guide sections"><div class="ed-wrap"><a href="#before">Before you start</a><a href="#installation-steps">Installation</a><a href="#check">Check it works</a><a href="#faq">Questions</a></div></nav><section class="ed-wrap guide-before" id="before"><div>${marineIcon('compass')}<h2>A quick check before you start.</h2></div><p>${e(g.requirement)}</p><div class="guide-route"><span>WHERE YOU WILL ADD IT</span><strong>${e(g.route)}</strong></div></section><section class="ed-wrap guide-instructions" id="installation-steps"><div class="guide-steps"><span class="ed-kicker">ONE PROJECT. ONE SNIPPET.</span><h2>Bring your site aboard.</h2><article><span class="guide-step-number">01</span><div><h3>Copy the right project’s snippet</h3><p>Open <a href="http://dashboard.orka.chat/">your Orka dashboard</a>, then Settings → your project → Installation. Under How to embed, click Copy Code. If this is a new website, create its project first so incoming messages have the right context.</p></div></article>${g.steps.map(([title,copy],i)=>`<article><span class="guide-step-number">${String(i+2).padStart(2,'0')}</span><div><h3>${e(title)}</h3><p>${e(copy)}</p></div></article>`).join('')}${g.id==='shopify'?`<a class="guide-app-link" href="https://apps.shopify.com/orka-live-chat" target="_blank" rel="noopener">${marineIcon('sail')} Prefer the app? Open Orka on the Shopify App Store ↗</a>`:''}</div><aside class="guide-install-aside">${snippetPanel()}<div class="guide-detail-note"><h3>The ${e(g.name)} detail that matters.</h3><p>${e(g.specific)}</p><p>${e(g.limit)}</p></div></aside></section><section class="ed-wrap guide-check" id="check"><div><span class="ed-kicker">THE FIRST HELLO IS YOUR TEST</span><h2>Installed is good.<br>Working is better.</h2><p>Use a real published page and a test visitor. You are checking the complete journey, from the widget to the right project in your inbox.</p></div><ol><li><strong>Open it like a customer.</strong><span>Visit the published site on desktop and mobile. Open the widget and check that it does not cover a key control.</span></li><li><strong>Send a recognizable test message.</strong><span>Use something like “Installation test — ${e(g.name)}”. Check it arrives in the project you intended.</span></li><li><strong>Answer from the shared inbox.</strong><span>Reply once and confirm the answer reaches the visitor. Then visit another site page and check again.</span></li><li><strong>Set the welcome before inviting people in.</strong><span>Review your colors, availability, reply expectations and help articles. Choose AI behavior separately.</span></li></ol></section><section class="ed-wrap guide-next-project"><img src="/assets/orky-head-v2.svg" width="75" height="60" alt="Orky"><div><h2>One more project? Same open tab.</h2><p>Repeat with the next project’s snippet. SaaS, app, shop or publication: the conversations can all come to one inbox.</p></div><a href="/best-live-chat-for-multiple-websites">Explore the portfolio workflow ↗</a></section>${faqSection(faqs,{title:`Your ${e(g.name)}<br>installation questions.`})}${faqSchema(faqs)}${guideFooter(g.sources)}`;
}
function productFacts(id){
 if(id==='orka')return {name:'Orka',price:'Solo free · Pod $29 / 3 people · Fleet $99 / 20',unit:'Monthly plans · AI usage separate',help:'Free multilingual help center, own domain, Markdown articles and in-widget Help on every plan.',watch:'Hosted only. Message search, image sharing and analytics start on Pod. Do not assume autonomous refunds or an enterprise contact center.',url:'/pricing',review:'/features'};
 const c=comparisons.find(c=>c.id===id),r=research[id];return {name:c.name,price:r.price,unit:r.unit,help:r.help,watch:r.watch[0]+'. '+r.watch[1]+'.',url:c.sources[0].url,review:'/'+id+'-review'};
}
export function bestBody(g){
 const faqs=[
  [`What is the best live chat for ${g.title}?`,`${productFacts(g.picks[0][0]).name} is our first option to evaluate for this guide’s stated scenario: ${g.scenario} That is an editorial judgment, not a universal winner. Use the other shortlist options when your requirements differ.`],
  [g.question,g.answer],
  ['Is Orka always the cheapest?','No. Some competitors have a free core product or a lower bundle price for a specific team. Compare the number of people, separately configured projects, knowledge features and AI usage. Orka’s main differentiation is one inbox with unlimited projects, not winning every price comparison.'],
  ['How do I compare AI costs fairly?','Use the same real questions and measure the full conversation. Orka’s calculator illustrates ten customer messages plus ten AI replies, including history and knowledge in input tokens. Other vendors may count resolutions, AI chats or credits. Those units are not directly interchangeable.'],
  ['Should I include a help center in the decision?','Yes. Check whether visitors can search published answers on a public site and inside the widget, whether your own domain and languages are supported, and which plan includes them. Orka includes its multilingual help center on every plan.'],
  ['Who wrote this guide, and how current is it?','Orka publishes this editorial shortlist and has a commercial interest in it. It uses linked official product and pricing documentation checked in September 2026. It is not an independent lab test or a customer-rating survey. Please email corrections to hello@orka.chat.']
 ];
 const sources=g.picks.flatMap(([id])=>id==='orka'?[['Orka plans and AI calculator','https://orka.chat/pricing'],['Orka feature catalogue','https://orka.chat/features']]:comparisons.find(c=>c.id===id).sources.slice(0,2).map(s=>[s.title,s.url]).concat([[productFacts(id).name+': workspace details',research[id].projectSource]]));
 return `<section class="c-hero best-hero"><div class="ed-wrap"><a class="c-back" href="/best-live-chat">← Choose by your situation</a><div class="c-hero-grid"><div class="c-hero-copy"><span class="ed-kicker">${e(g.kicker)}</span><h1>Best live chat for<br><em>${e(g.title)}.</em></h1><h2>${e(g.headline)}</h2><p>${e(g.lead)}</p><a class="ed-button lime" href="#shortlist">Explore the shortlist ↓</a><p class="c-disclosure">An editorial guide by Orka · September 2026</p></div><figure class="c-hero-scene"><img src="/assets/comparison-${g.scene}-v2.jpg" width="1536" height="1024" alt="Marine illustration: different vessels sharing a support harbor"><figcaption>${marineIcon('compass')} THE RIGHT TOOL DEPENDS ON YOUR COURSE.</figcaption></figure></div></div></section><section class="ed-wrap best-scenario"><span class="ed-kicker">WHAT “BEST” MEANS HERE</span><h2>A shortlist with a stated purpose.</h2><p>${e(g.scenario)}</p><small>Order reflects our judgment for that scenario. No invented test scores, review ratings or universal winner.</small></section><section class="ed-wrap best-quick"><h2>Find your route in a minute.</h2><div>${g.picks.map(([id,label],i)=>{const p=productFacts(id);return `<a href="#pick-${id}"><span class="best-rank">0${i+1}</span>${vendorMark({id,name:p.name},38)}<div><strong>${e(p.name)}</strong><small>${e(label)}</small></div><b>↘</b></a>`}).join('')}</div></section><section class="ed-wrap best-criteria"><span class="ed-kicker">THREE THINGS TO TRY BEFORE YOU COMMIT</span><h2>Give the trial a real job.</h2><div>${g.criteria.map(([title,copy],i)=>`<article>${marineIcon(['compass','anchor','shell'][i])}<h3>${e(title)}</h3><p>${e(copy)}</p></article>`).join('')}</div></section><section class="ed-wrap c-shortlist best-picks" id="shortlist">${g.picks.map(([id,label,why],i)=>{const p=productFacts(id);return `<article class="c-alt-card ${id==='orka'?'best-orka':''}" id="pick-${id}"><div class="c-alt-card-heading"><span class="c-alt-number">0${i+1}</span>${vendorMark({id,name:p.name},46)}<div><span class="ed-kicker">${e(label)}</span><h2>${e(p.name)}</h2></div></div><p class="best-why">${e(why)}</p><div class="c-alt-facts"><div><strong>Budget to consider</strong><p>${e(p.price)}</p><small>${e(p.unit)}</small></div><div><strong>Knowledge &amp; self-service</strong><p>${e(p.help)}</p></div><div><strong>Before you commit</strong><p>${e(p.watch)}</p></div></div><div class="best-card-links"><a href="${p.review}">${id==='orka'?'Explore Orka features':'Read our '+e(p.name)+' review'} ↗</a>${link(p.url,'Current pricing')}</div></article>`}).join('')}</section><section class="ed-wrap best-editor-note"><div>${marineIcon('lighthouse')}<span class="ed-kicker">THE DECISION WE WOULD NOT SKIP</span></div><h2>Your Tuesday matters more than a ranking.</h2><p>${e(g.advice)}</p><p>Trial the tool with the people who will answer, the projects they support and the difficult questions they actually receive. Keep a note of what requires another tab, another paid plan or an extra manual handover. Those details are more useful than a long list of checkmarks.</p><a href="/pricing#ai-pricing">Estimate Orka’s AI cost in conversations ↗</a></section>${faqSection(faqs,{title:'The questions behind<br>the shortlist.'})}${faqSchema(faqs)}${guideFooter(sources)}<section class="ed-wrap guide-related"><h2>A different situation?</h2><div>${bestChatGuides.filter(x=>x.id!==g.id).map(x=>`<a href="/${bestSlug(x)}">${marineIcon('sail')} Best live chat for ${e(x.title)} ↗</a>`).join('')}</div></section>`;
}
export function guidePages(){return [
 ...installationGuides.map(g=>({slug:installSlug(g),title:`How to add Orka to ${g.name}`,description:`Install Orka on ${g.name}: where to paste your project snippet, platform requirements, testing steps and troubleshooting. September 2026 official sources.`,body:installationBody(g)})),
 ...bestChatGuides.map(g=>({slug:bestSlug(g),title:`Best live chat for ${g.title} — September 2026`,description:g.lead,body:bestBody(g)})),
 {slug:'installation',title:'Install Orka on your website',description:'Step-by-step Orka installation for WordPress, Shopify, Webflow, Squarespace, Wix, Framer, WooCommerce, Ghost, Carrd and BigCommerce.',body:`<section class="guide-hub-hero ed-wrap"><span class="ed-kicker">YOUR NEXT HELLO STARTS HERE</span>${marineIcon('anchor')}<h1>Every website.<br><em>A way aboard.</em></h1><p>Choose your platform. Copy your project’s snippet.<br>Bring another product into the same support inbox.</p></section><section class="ed-wrap guide-platform-grid" aria-label="Installation guides">${installationGuides.map(g=>`<a href="/${installSlug(g)}">${platformPill(g)}<h2>${e(g.name)}</h2><p>${e(g.route)}</p><span>Installation guide ↗</span></a>`).join('')}</section><section class="ed-wrap guide-generic"><span class="ed-kicker">A CUSTOM SITE OR A VIBE-CODED APP?</span><h2>It starts with the same snippet.</h2><p>Open your project in Orka → Settings → Installation and copy the full code. Add it once in the site-wide HTML head or your framework’s equivalent script-loading setup. With an AI coding assistant, ask it to preserve the project ID, avoid duplicate loading and check a published page. For client-side routing, test navigation after the initial load.</p><a href="http://dashboard.orka.chat/">Open your Orka dashboard ↗</a><p>Do not paste private provider API keys into your site. The installation screen gives you the widget code you need.</p></section>`},
 {slug:'best-live-chat',title:'Best live chat: choose for the way you work',description:'Eight practical live-chat shortlists for founders, Shopify, SaaS, WordPress, agencies, small businesses, multiple websites and vibe-coded projects.',body:`<section class="guide-hub-hero ed-wrap"><span class="ed-kicker">DIFFERENT CREWS. DIFFERENT COURSES.</span>${marineIcon('compass')}<h1>The best live chat?<br><em>Start with your day.</em></h1><p>A portfolio of apps is not a single Shopify store.<br>Pick your situation for a shortlist with honest trade-offs.</p><small>Editorial guides by Orka · September 2026 · No universal winner</small></section><section class="ed-wrap best-directory">${bestChatGuides.map((g,i)=>`<a href="/${bestSlug(g)}"><span class="best-directory-number">0${i+1}</span>${marineIcon(['sail','buoy','anchor','shell'][i%4])}<h2>Best live chat for<br>${e(g.title)}</h2><p>${e(g.headline)}</p><span>Find your shortlist ↗</span></a>`).join('')}</section><section class="ed-wrap guide-generic"><h2>Why Orka is not first on every page.</h2><p>Because your requirements matter. We favor Orka for founders and small teams running several products in one inbox. We put other tools first when commerce operations, a broad single workspace, deployment control or a free human-chat starting point better match the stated scenario.</p><p>These are Orka’s editorial recommendations from official documentation, not independent testing or an exhaustive market ranking. Read the linked sources and use your own trial before committing.</p><a href="/compare">Explore all side-by-side comparisons ↗</a></section>`}
 ];}
export const guideSlugs=['installation','best-live-chat',...installationGuides.map(installSlug),...bestChatGuides.map(bestSlug)];
