import {escapeHtml as e,signup,marineIcon,faqSection,faqSchema} from './site-chrome.mjs';

export const sdkDocs='https://penida.gitbook.io/orka-livechat/how-to-add-orka-.../how-to-add-orka-on-your-single-page-application-spa';
export const shopifySdkDocs='https://penida.gitbook.io/orka-livechat/how-to-add-orka-.../how-to-add-orka-on-your-shopify-app';
const apple='https://apps.apple.com/fr/app/orka-chat/id6473671891';
const play='https://play.google.com/store/search?q=orka%20chat%20penida&c=apps';
const code=(id,title,content)=>`<div class="guide-snippet sdk-code"><div><span>${title}</span><button type="button" data-copy-snippet="${id}">Copy example</button></div><pre tabindex="0"><code id="${id}" tabindex="-1">${e(content)}</code></pre></div>`;
const questions=(items,title)=>faqSection(items,{title})+faqSchema(items);
const cta=()=>`<section class="ed-wrap app-final">${marineIcon('orca')}<h2>More context.<br>A better first answer.</h2><p>One inbox for every app, store and idea you run.</p><a class="ed-button lime" href="${signup}">Start with Orka ↗</a></section>`;
const reference=(items)=>`<section class="ed-wrap guide-sources"><span class="ed-kicker">STRAIGHT FROM THE DOCUMENTATION</span><h2>The details, when you need them.</h2><p>Checked in September 2026. Questions or corrections? <a href="mailto:hello@orka.chat">hello@orka.chat</a>.</p><ul>${items.map(([name,url])=>`<li><a href="${url}" target="_blank" rel="noopener">${name} ↗</a></li>`).join('')}</ul></section>`;

export function storeLinks(platform='both'){
 return `<div class="orka-store-links">${platform!=='android'?`<a href="${apple}" target="_blank" rel="noopener"><img src="/assets/mobile-apps/app-store-badge.svg" width="150" height="50" alt="Download Orka Chat on the App Store"></a>`:''}${platform!=='ios'?`<a href="${play}" target="_blank" rel="noopener" aria-label="Find Orka Chat on Google Play"><img src="/assets/mobile-apps/google-play-badge.png" width="170" height="66" alt="Google Play"></a>`:''}</div>`;
}
export function visitorDataDemo(){
 const rows=[['shop','studio-fern.myshopify.com'],['shop_name','Studio Fern'],['pricing_type','per-usage'],['plan','Pro'],['per_usage_plan','tier_1000'],['premium','Yes'],['has_branding','No'],['installed_at','2026-09-18'],['shopify_plan','Basic'],['admin_link','Open customer record ↗'],['onboarding','Calendar connected']];
 return `<div class="sdk-visitor-demo"><div class="sdk-visitor-head"><span class="sdk-demo-avatar">${marineIcon('orca')}</span><div><b>Studio Fern</b><small>merchant@example.com</small></div><span class="sdk-online">● Online</span></div><div class="sdk-data-label"><span>▥</span><strong>Visitor Data</strong><span>⌄</span></div><dl>${rows.map(([key,value])=>`<div><dt>${key}</dt><dd>${value}</dd></div>`).join('')}</dl><div class="sdk-add-field"><span>＋</span> Your next useful field <span>Any relevant value</span></div><p class="app-demo-caption">Fictional merchant, email and account values. Your app sends the fields you choose.</p></div>`;
}
const updateExample=`// After your Orka widget has loaded.
window.Orka.update({
  name: "Alex Morgan",
  email: "alex@example.com",
  shop_name: "Studio Fern",
  plan: "Pro",
  shopify_plan: "Basic",
  installed_at: "2026-09-18",
  pricing_type: "per-usage",
  per_usage_plan: "tier_1000",
  premium: "Yes",
  onboarding: "Calendar connected"
});`;
export function sdkBody(){
 const faqs=[
  ['Does Orka have a JavaScript SDK?','Yes. Orka provides the orka-web-sdk npm package and a browser API through window.Orka. Use them to initialize the widget, update visitor data, open or hide chat and connect support to your product.'],
  ['Which custom visitor attributes can I send?','Choose the support-relevant key-value fields your app knows: a plan, installation date, usage tier, account role, language, feature state or internal record link. You are not restricted to the example fields on this page. Check how your chosen values render in Visitor Data.'],
  ['Is visitor data imported automatically from Shopify?','Your integration supplies it. The SDK does not grant Shopify API access or determine a subscription on its own. Read account data through your app’s existing authenticated flow, then pass the fields that help your team.'],
  ['Can I change the data after the user upgrades?','Yes. Call update with fresh values after a plan change or other relevant event. Initial ORKA_SESSION data belongs before the widget script; update is the way to provide later changes.'],
  ['Can a non-developer connect visitor data with AI?','An AI coding assistant can help locate the account data and add the integration. Give it the official docs and the field names you want. Someone should still review the data mapping and test account changes before publishing.'],
  ['Does reset securely sign a visitor out?','The documented reset method closes chat and refreshes the current page title and URL. Do not treat that as a guaranteed identity wipe. Design and test your account-switching and logout flow separately.'],
  ['Is this a native iOS or Android SDK?','The package described here is a JavaScript web SDK. It works with your website or web app. A web-based support view inside a mobile app needs its own integration and testing; Orka’s mobile operator apps are a separate way for your team to answer.']
 ];
 return `<section class="ed-wrap sdk-hero"><div><span class="ed-kicker">ORKA JAVASCRIPT SDK</span><h1>Live chat with<br><em>your user data.</em></h1><p class="ed-lead">The plan they pay for. The feature they are using. The detail that makes the answer obvious.</p><p>Connect the information your product already knows to Orka’s Visitor Data panel. Your team sees it alongside the conversation.</p><a class="ed-button lime" href="#connect-data">Connect your data ↓</a><div class="sdk-hero-tags"><span>npm · orka-web-sdk</span><span>Custom attributes</span><span>SPAs + Shopify apps</span></div></div>${visitorDataDemo()}</section><section class="ed-wrap sdk-use-cases"><span class="ed-kicker">YOUR PRODUCT. YOUR CONTEXT.</span><h2>Not a fixed checklist.<br>The fields your team actually needs.</h2><div>${[
 ['shark','Shopify apps','Shop domain, Shopify plan, app plan, billing type, installation date, branding setting and a link to your internal admin.'],
 ['orca','SaaS & vibe-coded apps','Workspace name, subscription, usage tier, onboarding step, enabled features and the screen where someone needs a hand.'],
 ['shell','Mobile & subscription products','App version, platform, language, plan and the state of a feature. Enough context to understand the issue before asking another question.']
 ].map(([icon,title,copy])=>`<article>${marineIcon(icon)}<h3>${title}</h3><p>${copy}</p></article>`).join('')}</div><p>You choose the keys and supply the values. An internal record link can save a lookup; a plan or installation date can change how you explain the next step. These are examples, not a limit on what you can model.</p></section><section class="sdk-connect-band" id="connect-data"><div class="ed-wrap sdk-connect-grid"><div><span class="ed-kicker">SEND THE CONTEXT. KEEP THE CONVERSATION.</span><h2>A few fields.<br>A much warmer hello.</h2><ol class="sdk-steps"><li><b>01</b><div><h3>Install your project’s widget</h3><p>Copy its snippet from Settings → your project → Installation, or use the npm package in your web app.</p></div></li><li><b>02</b><div><h3>Map the useful attributes</h3><p>Take account values from your own trusted app state. Pass them with the SDK instead of asking the customer to type them again.</p></div></li><li><b>03</b><div><h3>Keep them up to date</h3><p>Refresh the context when the account, plan or relevant screen changes. Check Visitor Data with a test account.</p></div></li></ol></div><div>${code('sdk-update-example','BROWSER SDK · EXAMPLE ATTRIBUTES',updateExample)}<p class="app-small-note">The fields are fictional examples. Keep passwords, API keys and payment credentials out of visitor attributes. Browser-supplied data is support context, not authorization for privileged actions.</p></div></div></section><section class="ed-wrap sdk-methods"><span class="ed-kicker">A SMALL API. A LOT OF POSSIBILITIES.</span><h2>Put support where it belongs.</h2><div>${[['update(data)','Refresh user and session attributes.'],['show() / hide()','Connect your own Help button to the widget.'],['toggle()','Open or close chat from a custom entry point.'],['reset()','Close chat and refresh page title/URL context.'],['destroy()','Remove the widget instance when appropriate.'],['ORKA_SESSION','Provide initial session data before the script loads.']].map(([name,copy])=>`<article><code>${name}</code><p>${copy}</p></article>`).join('')}</div><p>Wait for the browser widget API before calling its methods. The npm wrapper can queue update data before the widget is ready. The <a href="/how-to-add-orka-to-single-page-application">SPA guide</a> covers initialization and navigation.</p></section>${questions(faqs,'Your SDK questions.<br>With real answers.')}${reference([['Official Orka SDK and SPA documentation',sdkDocs],['Orka integration for Shopify apps',shopifySdkDocs],['Published orka-web-sdk package','https://www.npmjs.com/package/orka-web-sdk']])}${cta()}`;
}

const spaClient=`// orka-client.js — called from the browser after mount.
let sdkPromise;

export function getOrka() {
  if (typeof window === "undefined") return Promise.resolve(null);
  if (!sdkPromise) {
    sdkPromise = import("orka-web-sdk").then(({ default: Orka }) => {
      Orka.configure("YOUR_PROJECT_ID");
      return Orka;
    });
  }
  return sdkPromise;
}`;
const spaUpdate=`// Once your client has the signed-in account data:
const orka = await getOrka();
orka?.update({
  name: "Alex Morgan",
  email: "alex@example.com",
  plan: "Pro",
  workspace: "Studio Fern",
  current_screen: "Billing"
});`;
const spaPrompt=`Add Orka to this single-page application using the official orka-web-sdk package.
Read these docs first: ${sdkDocs}

Use my project ID: YOUR_PROJECT_ID
1. Identify the app's persistent root and client-only lifecycle.
2. Initialize Orka once. Use the package's default export. Do not import browser-only code during server rendering.
3. Send the support fields I choose from existing account state with update(). Do not invent data or include API keys, access tokens or payment credentials.
4. Update useful context after route or account changes. Do not claim reset() securely signs a user out; review account switching separately.
5. Avoid duplicate widgets on navigation or remount. Preserve the current layout and consent settings.
6. Show the changed files and a test plan for navigation, messages, replies and logout.

Fields I want to show in Visitor Data: [LIST YOUR FIELD NAMES HERE]`;
function spaBody(){
 const faqs=[
  ['What is a single-page application?','A web app that changes screens in the browser without loading a completely new HTML document for every click. Many React, Vue and Angular apps work this way, and frameworks such as Next.js or Remix can combine server rendering with client navigation.'],
  ['Who benefits from adding Orka to a SPA?','SaaS founders, Shopify app developers, product teams, agencies and people building with AI. It is especially useful when customers need support inside a signed-in dashboard and your team needs account context.'],
  ['Is adding Orka to a SPA difficult?','The basic integration is small: install the package, configure your project once in the browser and test a conversation. Connecting account fields or handling server rendering and logout needs more care. The exact effort depends on your app.'],
  ['Can I do it with an AI coding assistant if I am not technical?','Yes, with a tool that can read and edit your project, or by applying its proposed changes. Give it the official documentation, your project ID and the fields you want. Ask it to explain and test navigation, duplicate loading and account changes before publishing.'],
  ['Can I use the ordinary installation snippet instead?','Yes. Install it once in the persistent shared layout and set initial ORKA_SESSION data before the widget script. Choose either the snippet or the npm integration so the same project is not loaded twice.'],
  ['Do I need to reload Orka on every route?','No. Keep the widget attached to the persistent app shell. Send changed visitor fields with update. If you use reset to refresh page title and URL, remember that it also closes the chat.'],
  ['How do I use Orka with server-side rendering?','The published package accesses window. Load it only in the browser, for example through a dynamic import after a client component mounts. Do not import it in server-executed code.'],
  ['Why does the example use a default import?','The published orka-web-sdk 1.0.4 source and type declarations export the SDK instance as default. Some older documentation examples show a named import. This guide uses the published package API checked in September 2026.'],
  ['Does reset erase the previous user’s identity?','Do not rely on it for that. The documented behavior is to close chat and send current page context. Test account switching and logout explicitly so the next person cannot inherit the wrong conversation. Ask Orka for the appropriate session flow if needed.'],
  ['Will Orka work with the other projects I run?','Create or select the right Orka project for each product. All those conversations can flow into one shared inbox. The free Solo plan includes unlimited projects; the team and AI options are listed on the pricing page.']
 ];
 return `<section class="ed-wrap spa-hero"><div><a class="c-back" href="/installation">← Installation guides</a><span class="ed-kicker">REACT · VUE · ANGULAR · YOUR NEXT WEB APP</span><h1>How to add Orka to your<br><em>Single Page Application.</em></h1><p class="ed-lead">Keep chat in the app as people move from screen to screen. Give your team the user context that makes support easier.</p><a class="ed-button lime" href="#spa-install">Follow the guide ↓</a><a class="app-secondary" href="#spa-ai">Doing it with AI? Start here ↗</a></div><div class="spa-route-art"><div class="spa-tabs"><span>Overview</span><span>Billing</span><span>Settings</span></div><div class="spa-route-flow"><span>/overview</span><span>↓</span><span>/billing</span><span>↓</span><span>/settings</span></div><div class="spa-stays"><img src="/assets/orka-logo.svg" width="45" height="42" alt="Orka"><strong>Your chat stays aboard.</strong><small>One app shell. One widget.</small></div></div></section><section class="ed-wrap sdk-use-cases"><span class="ed-kicker">FOR THE PEOPLE BUILDING THE PRODUCT</span><h2>They need help in your app.<br>Not another place to explain it.</h2><div>${[['orca','SaaS & member dashboards','Answer questions about a plan, workspace or feature while the person is using it. Pass those details to Visitor Data.'],['shark','Shopify & product portfolios','Keep each app’s identity clear while the same small team handles all the conversations in one inbox.'],['shell','Vibe-coded products','Your AI assistant can add the integration to the app it helped build. Give it the docs and test the result, just as you test the rest of your product.']].map(([icon,title,copy])=>`<article>${marineIcon(icon)}<h3>${title}</h3><p>${copy}</p></article>`).join('')}</div></section><section class="ed-wrap spa-explainer"><h2>What makes a SPA different?</h2><p>A traditional site loads another document when you follow a link. A single-page app often keeps the same document and replaces the content of the screen. That means a chat widget belongs in the persistent application shell, rather than in a component that is recreated for every route.</p><p>You do not need to become a framework expert to understand the goal: one widget, the right project, useful customer data and a conversation that remains available while someone navigates. React, Vue and Angular can all use the JavaScript SDK; a framework that renders on the server needs a browser-only initialization step.</p></section><section class="ed-wrap spa-install" id="spa-install"><span class="ed-kicker">THE PRACTICAL SETUP</span><h2>From package to first reply.</h2><article><div><b class="sdk-step-number">01</b><h3>Choose the right project</h3><p>Open Orka → Settings → your project → Installation. Use that project’s ID. The example below uses <code>YOUR_PROJECT_ID</code>, not a real account identifier. Create separate projects for products whose support context should remain separate.</p></div>${code('spa-package','INSTALL IN YOUR WEB APP','npm install orka-web-sdk')}</article><article><div><b class="sdk-step-number">02</b><h3>Initialize once, in the browser</h3><p>Call this helper from your persistent client root after it mounts. The cached promise prevents repeated configuration when routes or components remount. For React, call it in a client effect; for Vue, in the persistent root’s mounted lifecycle; for Angular, in a browser-only application service.</p><p>The package reads <code>window</code>, so keep its import out of server rendering. This example matches the default export in the published package.</p></div>${code('spa-client','CLIENT-ONLY INITIALIZATION',spaClient)}</article><article><div><b class="sdk-step-number">03</b><h3>Give the conversation context</h3><p>Once your app knows who is signed in, supply the fields your team needs. Your own data provides the values: a subscription, installation date, usage tier or current feature. Call <code>update</code> again when relevant values change.</p><p>These attributes help with support; they are not a replacement for authentication or server-side permissions. <a href="/sdk">Explore custom visitor data ↗</a></p></div>${code('spa-update','EXAMPLE VISITOR DATA',spaUpdate)}</article><article><div><b class="sdk-step-number">04</b><h3>Connect navigation deliberately</h3><p>Keep the widget mounted while people change screens. Use <code>update</code> for changing attributes. The browser <code>reset()</code> method refreshes the current page title and URL but also closes chat, so choose when that behavior is useful.</p><p>You can open chat from a Help button with the browser SDK once it has loaded. Avoid inserting a second installation script on every click.</p></div><div class="spa-lifecycle"><span>Application starts <b>Initialize once</b></span><span>Plan or feature changes <b>Update the context</b></span><span>Help button clicked <b>Open the chat</b></span><span>Account changes <b>Test the session boundary</b></span></div></article></section><section class="sdk-connect-band" id="spa-ai"><div class="ed-wrap sdk-connect-grid"><div><span class="ed-kicker">YOU CAN ASK YOUR AI TO DO THE CODE</span><h2>Not technical?<br>Give it a precise brief.</h2><p>Claude, ChatGPT, Gemini or Grok can help you find the shared layout and add Orka. A coding tool with access to the repository can propose the change directly; a chat-only assistant may give you files to copy.</p><p>Replace the placeholder ID, list the fields you want and ask it to explain its changes. Then test a real conversation. A confident “done” from the assistant is not the same as a working integration.</p><a href="/how-to-install-orka-with-ai">More about installing with AI ↗</a></div>${code('spa-ai-prompt','A BRIEF YOU CAN COPY',spaPrompt)}</div></section><section class="ed-wrap spa-checklist"><span class="ed-kicker">BEFORE YOU SHIP</span><h2>A short test. A calmer launch.</h2><div>${[['One widget','Navigate through several routes and use Back/Forward. Confirm the launcher is not duplicated.'],['The right person','Check Visitor Data with test accounts, then change the plan or relevant app state.'],['A full conversation','Send a message, answer from Orka and confirm the reply returns to the customer.'],['A clean handover','Test sign-out and account switching. Do not assume reset alone removes identity or conversation access.'],['Real devices','Try a narrow screen, a refresh on a deep URL and any consent rules your app uses.'],['A useful fallback','If a call runs too early, wait for the widget API. Check the project ID, network and browser console if chat does not appear.']].map(([title,copy])=>`<article><span>✓</span><h3>${title}</h3><p>${copy}</p></article>`).join('')}</div></section>${questions(faqs,'Your SPA questions.<br>Before the first hello.')}${reference([['Orka: SPA integration and SDK methods',sdkDocs],['Orka: Shopify and Remix integration',shopifySdkDocs],['orka-web-sdk on npm','https://www.npmjs.com/package/orka-web-sdk']])}${cta()}`;
}

export function mobileAppExamples(){
 const apps=[['fish-spots','Fish Spots','Fishing forecasts & spot insights','https://apps.apple.com/fr/app/fish-spots-app-de-p%C3%AAche-ia/id6792068884','“How do I read the forecast for my saved spot?”','Show the app version, plan and relevant feature in Visitor Data. Link the forecasting help article without collecting a precise fishing location for a general question.'],['garden-ai','Garden AI','AI garden & outdoor design','https://apps.apple.com/fr/app/jardin-ai-am%C3%A9nagement-3d-ia/id6751772833','“How do I redesign my terrace from a photo?”','Give support the app version, language and plan. Explain the photo-to-design workflow from your knowledge base, then involve a person if the result needs a closer look.']];
 return `<section class="ed-wrap mobile-examples real-mobile-examples"><span class="ed-kicker">REAL APPS. RELATABLE SUPPORT QUESTIONS.</span><h2>From a better fishing day<br>to a dream garden.</h2><p>Two different products. Two different sets of questions. The same way to bring a customer’s context into one support inbox.</p><div>${apps.map(([id,name,sub,url,q,copy])=>`<article><a class="mobile-real-title" href="${url}" target="_blank" rel="noopener"><img src="/assets/mobile-apps/${id}-icon.jpg" alt="${name} app icon" width="64" height="64" loading="lazy"><span><h3>${name}</h3><small>${sub}</small></span><b>↗</b></a><div class="mobile-store-shots"><img src="/assets/mobile-apps/${id}-screen-1.jpg" alt="${name}: official App Store preview" loading="lazy" width="240" height="480"><img src="/assets/mobile-apps/${id}-screen-2.jpg" alt="${name}: product screen from its App Store listing" loading="lazy" width="240" height="480"></div><h4>${q}</h4><p>${copy}</p><a href="${url}" target="_blank" rel="noopener">See the real app on the App Store ↗</a></article>`).join('')}</div><p class="app-demo-caption">Official app artwork from the linked App Store listings. Support questions are illustrative, not customer quotes or a claim about a specific implementation. The linked listings are for iOS; they do not establish Android availability.</p></section>`;
}

function phoneInbox(platform){return `<div class="operator-scene"><span class="operator-scene-orbit" aria-hidden="true">${marineIcon('orca')}</span><div class="operator-phone"><div class="operator-status">9:41 <span>●●● ▰</span></div><div class="operator-top"><img src="/assets/orka-logo.svg" width="26" height="24" alt="Orka"><strong>Inbox</strong><span>⋯</span></div><div class="operator-tabs"><b>All projects</b><span>Assigned to me</span></div><div class="operator-live"><i></i> Keep up with your conversations</div>${[['AM','Studio Fern','Can you help me finish setup?','2m','fern'],['JR','Northstar App','I have a question about my plan.','4m','north'],['SK','Little Tide Store','Which size should I choose?','8m','tide']].map(([initial,project,message,time,style])=>`<div class="operator-message"><span class="operator-avatar ${style}">${initial}</span><div><strong>${project}<small>${time}</small></strong><p>${message}</p><span>Open conversation</span></div></div>`).join('')}<div class="operator-note"><strong>Emma · private note</strong><p>I checked the setup. Ready to pick this up.</p></div><div class="operator-phone-nav"><span>◉<small>Inbox</small></span><span>♧<small>Team</small></span><span>⚙<small>Settings</small></span></div><div class="app-phone-home"></div></div><div class="operator-float"><span>YOUR ${platform==='ios'?'IPHONE':'ANDROID PHONE'}</span><strong>Same crew.<br>More freedom.</strong></div><p class="app-demo-caption">Illustration with fictional people, projects and messages. Layout may differ by app version.</p></div>`;}
function operatorBody(platform){
 const ios=platform==='ios',name=ios?'iOS':'Android';
 const faqs=[
  [`What is Orka Chat for ${name}?`,`It is the mobile app for the people answering customer conversations in Orka. Use it to follow support when you step away from your computer, rather than opening a separate support tool for every product.`],
  ['Is this the same as putting a chat in my own mobile app?','No. This page is about the Orka app your support team uses. Embedded visitor chat is the experience you put inside your own product. There are separate guides for adding web-based support to iOS, Android and other mobile apps.'],
  ['Can I follow conversations in real time?','Use the mobile app with an internet connection to keep up with incoming conversations and reply while you are away from your desk. Delivery and alerts depend on connectivity, the app state and your device notification settings.'],
  ['Do I have to be available all the time?','No. Mobile access is a choice, not a promise of round-the-clock human support. Keep your availability and response expectations honest, and use your team or configured Orky coverage when you are away.'],
  ['Can I use my existing Orka account?','Yes. Sign in with the Orka account you use for your projects. Your team’s project access still determines the workspace you should use.'],
  ['Are mobile apps included in the free plan?','Yes. Orka includes iOS and Android apps on every plan, including Solo. Team limits and AI usage are separate from downloading the operator app.'],
  ['Are the people and messages shown here real customer data?','No. The illustration uses fictional people, projects and messages. It demonstrates the workflow without publishing customer identities or account information.'],
  [ios?'Where do I download Orka for iPhone?':'Where does the Google Play button go?',ios?'Use the App Store button on this page to open the official Orka Chat listing. The store shows the current compatibility requirements for your device.':'The Google Play button opens the Orka Chat search supplied by the team. Look for the Orka Chat listing from PENIDA. Store results and availability can vary by country or device; contact hello@orka.chat if you cannot find it.']
 ];
 return `<section class="ed-wrap operator-hero"><div><img class="operator-app-icon" src="/assets/mobile-apps/orka-ios-icon.jpg" width="78" height="78" alt="Orka Chat app icon"><span class="ed-kicker">ORKA CHAT FOR ${name.toUpperCase()}</span><h1>Your customer support.<br><em>On your ${ios?'iPhone':'Android phone'}.</em></h1><p class="ed-lead">Keep up with your projects in real time. Read the conversation, send a reply and carry on with your day.</p><p>The mobile app for the humans behind your SaaS, apps and stores. Your support crew travels with you.</p>${storeLinks(platform)}${!ios?'<small class="store-search-note">Opens Google Play search for Orka Chat by PENIDA.</small>':''}<a class="app-secondary" href="/${ios?'android':'ios'}-app">Also on ${ios?'Android':'iOS'} ↗</a></div>${phoneInbox(platform)}</section><section class="ed-wrap operator-benefits"><span class="ed-kicker">STEP AWAY FROM THE DESK. KEEP THE CONTEXT.</span><h2>A quick reply.<br>Without unpacking your laptop.</h2><div>${[['orca','Stay close to the conversation','Check what came in while you were out. Read the latest messages and answer with the customer’s question in front of you.'],['shark','Keep an eye on what matters','Follow active support and check the conversations that need a person. A launch-day question does not have to wait for your next desktop session.'],['anchor','Keep your crew in the loop',ios?'Add internal notes and organize conversations with statuses, as described in the iOS app listing. Leave a useful handover when someone else should take the next step.':'Continue the team’s support workflow from your phone. Keep the conversation in Orka so a teammate can pick it up with its context.']].map(([icon,title,copy])=>`<article>${marineIcon(icon)}<h3>${title}</h3><p>${copy}</p></article>`).join('')}</div></section><section class="operator-day"><div class="ed-wrap"><div><span class="ed-kicker">SUPPORT THAT FITS A REAL DAY</span><h2>Morning launch.<br>Afternoon outside.</h2><p>Check in between meetings, answer a simple setup question during a break or see whether a teammate has picked up the tricky one. The point is to make support easier to reach when you choose to be available.</p><p>Keep the longer investigation for the right moment. Set clear response expectations and let Orky handle the questions covered by your configured knowledge when that suits your project.</p></div><ol><li><span>01</span><div><strong>Download Orka Chat</strong><p>${ios?'Open the official App Store listing.':'Open the Google Play search and select Orka Chat by PENIDA.'} Check the store’s current requirements for your device.</p></div></li><li><span>02</span><div><strong>Sign in to your workspace</strong><p>Use your existing Orka account. Check that you can see the projects you support.</p></div></li><li><span>03</span><div><strong>Make a test round trip</strong><p>Send a test visitor message, reply from the phone and confirm it arrives. Review the notification permissions and connection your device needs.</p></div></li></ol></div></section><section class="ed-wrap operator-distinction"><div>${marineIcon('buoy')}<h2>Answer from your phone.<br>Or put chat inside your app.</h2></div><p>Those are two different jobs. Orka’s ${name} operator app lets your team handle support on the go. If you build a mobile product and want its users to contact you without leaving it, explore our <a href="/best-live-chat-for-${ios?'ios':'android'}-apps">in-app support guide</a> and <a href="/sdk">visitor-data SDK</a>.</p></section>${questions(faqs,`Orka on ${name}.<br>Your questions, answered.`)}${reference(ios?[['Orka Chat — official App Store listing',apple]]:[['Orka Chat — Google Play search',play],['Orka mobile features and plans','https://orka.chat/features']])}<section class="ed-wrap operator-download"><img src="/assets/orka-logo.svg" width="48" height="44" alt="Orka"><h2>One inbox.<br>In your pocket.</h2>${storeLinks()}<p>Mobile apps included on every plan. <a href="/pricing">See all plans ↗</a></p></section>`;
}
export const integrationSlugs=['sdk','how-to-add-orka-to-single-page-application','ios-app','android-app'];
export function integrationPages(){return [
 {slug:'sdk',title:'JavaScript live chat SDK & custom visitor data — Orka',description:'Connect your user data to Orka: plans, Shopify merchant details, installation dates, usage tiers and custom attributes beside every support conversation.',body:sdkBody()},
 {slug:'how-to-add-orka-to-single-page-application',title:'How to add Orka live chat to a Single Page Application (SPA)',description:'Install Orka in React, Vue, Angular or a server-rendered web app. JavaScript SDK examples, visitor data, navigation, an AI prompt and a practical test checklist.',body:spaBody()},
 {slug:'ios-app',title:'Orka Chat for iOS — customer support on your iPhone',description:'Download Orka Chat on the App Store. Follow customer conversations, reply on the go and keep your support team in the loop from your iPhone.',body:operatorBody('ios')},
 {slug:'android-app',title:'Orka Chat for Android — live customer support on your phone',description:'Find Orka Chat on Google Play. Follow your customer conversations and reply from your Android phone, with one support inbox for your projects.',body:operatorBody('android')}
 ];}
